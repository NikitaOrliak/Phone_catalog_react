import React from 'react';
import './ErrorMessage.scss';

export const ErrorMessage: React.FC = () => (
  <div className="errorMessage">
    <h3 className="errorMessage__text">Something went wrong</h3>
  </div>
);
